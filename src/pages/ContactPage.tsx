import React from 'react';
import Header from "../components/Header/Header";

const ContactPage: React.FC = () => {
    return (
        <main>
            <Header></Header>
            <div> Контакты жи есть </div>
        </main>
    );
};

export default ContactPage;